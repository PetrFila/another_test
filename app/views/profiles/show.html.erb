<p id="notice"><%= notice %></p>


<div class="row">
  <div class=col-lg-3></div>
    <div class="card border-light col-lg-6">
      <h3 class="card-header text-center"><%= full_name %></h3>
      <div class="card-body">
        <h4 class= "card-title"><%= @profile.title %></h4>
        <p class="card-text"><%= @profile.about %></p>
      </div>
    </div>
  <div class=col-lg-3></div>
</div>






<div class="row">
  <div class=col-lg-3></div>
    <div class="col-lg-6">
      <%= link_to 'Edit', edit_profile_path(@profile) %> |
      <%= link_to 'Back', profiles_path %>
    </div>
  <div class=col-lg-3></div>
</div>

<!-- Messaging form -->
<div class="form-group col-lg-12 col-md-12 col-sm-12">
  <%#= form.text_area :message, type: "text", class: "form-control", placeholder: "Write your message here..."%>
</div>

<%#= form.submit 'Send message', type: "submit", class: "btn btn-primary" %>



<!-- A button to schedule a session. It will take you to another page -->
<%#= form.submit 'Send message', type: "submit", class: "btn btn-primary" %>


<!-- A button to start a session -->
<%#= form.submit 'Send message', type: "submit", class: "btn btn-primary" %>
